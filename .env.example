# --- Supabase (already in Vercel — copy values here for local dev) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=        # server-only, never exposed to the client

# --- Existing contact vars (already in Vercel) ---
OWNER_EMAIL=
OWNER_PHONE=

# --- New: delivery distance (server-only, Routes API enabled) ---
GOOGLE_MAPS_API_KEY=

# --- New: email (Resend) ---
RESEND_API_KEY=
EMAIL_FROM=

# --- New: SMS (Twilio; behind a toggle until A2P 10DLC approval) ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
SMS_ENABLED=false                 # flip to true once A2P registration clears

# --- New: notification recipients ---
NOTIFY_PHONES=4403763470,4405208414
NOTIFY_EMAILS=evanslandscapingandplowing@gmail.com
